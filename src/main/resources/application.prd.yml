spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    passoword: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create